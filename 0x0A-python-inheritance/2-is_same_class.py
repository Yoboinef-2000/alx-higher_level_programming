#!/usr/bin/python3

"""I am forced to have this line."""


def is_same_class(obj, a_class):
    """
    Return true if obj is instance of a_class.

    Return false otherwise.
    """
    if type(obj) is a_class:
        return (True)
    else:
        return (False)
