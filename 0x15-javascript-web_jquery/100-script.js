window.onload = function () {
  const header = document.querySelector('header');
  header.style.color = '#FF0000';
};

// If I am not mistaken, this way, the script must
// wait for the enitre page to load ('window.onload')
// before executing.

// Trust
// God bless.
